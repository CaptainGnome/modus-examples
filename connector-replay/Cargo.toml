[package]
name = "connector"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
modus-sdk = { path = "../../modus-sdk/guest", default-features = false, features = ["connector"] }

[profile.release]
lto = true
opt-level = "s"
panic = "abort"
