[package]
name = "widget"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
modus-sdk = { path = "../../modus-sdk/guest", default-features = false, features = ["widget"] }

[profile.release]
lto = true
opt-level = "s"
panic = "abort"
